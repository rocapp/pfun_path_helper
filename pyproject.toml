[tool.poetry]
name = "pfun-path-helper"
version = "0.0.3"
description = "Path helper script"
authors = ["Robbie Capps <robbie@pfun.me>"]
packages = [{include = "pfun_path_helper"}]

[tool.poetry.dependencies]
python = "^3.10"
build = "^0.10.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4.2"
ipython = "^8.16.1"
tox = "^3.23.0"

[tool.poetry.group.dev.dependencies]
tox-poetry-installer = {extras = ["poetry"], version = "^0.10.3"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
